import { starsSmall, starsMedium, starsBig } from 'src/styles/backgroundStyles'

const Background = () => (
  <>
    <div css={starsSmall} />
    <div css={starsMedium} />
    <div css={starsBig} />
  </>
)

export default Background
